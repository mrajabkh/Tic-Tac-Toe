Assuming you have npm already installed, run the following commands into the terminal.
npm install -g parcel
npm install react-dom

Then run "parcel index.html" and open the website on the link "http://localhost:1234/" in order to see the project.
